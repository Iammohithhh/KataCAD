# Backend API URL (without trailing slash)
NEXT_PUBLIC_API_URL=http://localhost:8000

# Booth mode — set to "true" for the exhibition build. Hides the
# developer-facing status surface, leaving only the visitor UI.
NEXT_PUBLIC_BOOTH_MODE=false
